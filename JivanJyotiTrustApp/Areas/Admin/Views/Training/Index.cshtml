@model List<JivanJyotiTrustApp.Models.Training>
<h2>Training List</h2>
<a asp-action="Create" class="btn btn-primary mb-3">Create Training</a>
<table class="table table-bordered">
<thead><tr><th>City</th><th>Location</th><th>Description</th><th>Actions</th></tr></thead>
<tbody>
@foreach (var item in Model) {
<tr><td>@item.City?.Name</td><td>@item.Location</td><td>@item.Description</td><td>
<a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
<a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
<a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
</td></tr>}
</tbody></table>
